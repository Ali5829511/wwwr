# إرشادات الأمان - نظام إدارة المرور والإسكان

## 🔒 نظرة عامة على الأمان

هذا النظام يحتوي على بيانات حقيقية لسكان وحدة إسكان هيئة التدريس بجامعة الإمام محمد بن سعود الإسلامية. يجب اتباع إرشادات الأمان التالية لحماية البيانات والخصوصية.

---

## 🔐 نظام المصادقة

### بيانات تسجيل الدخول الافتراضية

النظام يأتي مع حسابات افتراضية للاختبار:

| الدور | اسم المستخدم | كلمة المرور الافتراضية |
|-------|--------------|------------------------|
| المدير | `admin` | `admin123` |
| موظف المخالفات | `violations` | `violations123` |
| الاستعلام | `inquiry` | `inquiry123` |

### ⚠️ تحذير أمني هام

**يجب تغيير كلمات المرور الافتراضية فوراً قبل النشر الرسمي!**

لتغيير كلمات المرور:
1. قم بتسجيل الدخول كمدير
2. انتقل إلى **إدارة المستخدمين**
3. قم بتحديث كلمات المرور لجميع الحسابات

---

## 🔑 إدارة مفاتيح API

### Plate Recognizer API

النظام يستخدم **Plate Recognizer API** للتعرف على لوحات السيارات. رمز API **لم يعد مضمناً في الكود** لأسباب أمنية.

#### كيفية تكوين رمز API:

**الطريقة 1: عبر واجهة الإعدادات (موصى بها)**
1. قم بتسجيل الدخول كمدير
2. انتقل إلى **الإعدادات والخدمات**
3. افتح **إعدادات Webhook**
4. أدخل رمز API الخاص بك في حقل **Plate Recognizer API Token**
5. احفظ الإعدادات

**الطريقة 2: عبر localStorage**
افتح Console في المتصفح وقم بتنفيذ:
```javascript
localStorage.setItem('PLATE_RECOGNIZER_API_TOKEN', 'YOUR_API_TOKEN_HERE');
```

**الطريقة 3: عبر ملف تكوين منفصل (للنشر على خادم)**
أنشئ ملف `config/api-keys.js` (مستثنى من Git):
```javascript
const API_KEYS = {
    PLATE_RECOGNIZER: 'YOUR_API_TOKEN_HERE'
};
```

### الحصول على رمز API

1. سجل حساب في [Plate Recognizer](https://platerecognizer.com)
2. اذهب إلى لوحة التحكم
3. انسخ رمز API الخاص بك
4. استخدمه في النظام حسب الطرق أعلاه

---

## 🛡️ حماية البيانات

### البيانات الحساسة في النظام

النظام يحتوي على البيانات التالية:

| نوع البيانات | العدد | الحساسية |
|--------------|-------|----------|
| السكان (أسماء، هواتف) | 1,057 | عالية ⚠️ |
| الوحدات السكنية | 1,134 | متوسطة |
| المباني | 165 | منخفضة |
| ملصقات السيارات | 2,382 | متوسطة |

### آلية الحماية

#### 1. نظام تسجيل الدخول
- جميع الصفحات محمية بنظام مصادقة
- يتم التحقق من تسجيل الدخول قبل عرض أي بيانات
- الجلسات محفوظة في `localStorage`

#### 2. الصلاحيات
- **المدير:** وصول كامل لجميع الميزات
- **موظف المخالفات:** إدارة المخالفات والاستعلامات
- **الاستعلام:** قراءة فقط

#### 3. حماية من جانب العميل
- التحقق من الصلاحيات في كل صفحة
- إعادة التوجيه التلقائي للمستخدمين غير المصرح لهم

---

## 🚨 التهديدات المحتملة والحلول

### 1. الوصول غير المصرح به
**التهديد:** محاولة الوصول إلى البيانات بدون تسجيل دخول

**الحل الحالي:**
- نظام مصادقة من جانب العميل
- التحقق من الجلسة في كل صفحة

**التوصية للنشر الرسمي:**
- إضافة مصادقة من جانب الخادم (Backend)
- استخدام JWT أو Session Tokens
- تشفير الاتصال عبر HTTPS

### 2. تسريب مفاتيح API
**التهديد:** كشف رمز Plate Recognizer API للعامة

**الحل المطبق:**
- إزالة الرمز من الكود المصدري
- تخزينه في localStorage أو ملفات تكوين منفصلة
- استثناء ملفات التكوين من Git

**التوصية:**
- استخدام متغيرات بيئة على الخادم
- عدم تضمين الرمز في الكود الأمامي

### 3. هجمات XSS (Cross-Site Scripting)
**التهديد:** حقن كود JavaScript ضار

**الحل الموصى به:**
- تنظيف جميع المدخلات من المستخدمين
- استخدام Content Security Policy (CSP)
- عدم استخدام `innerHTML` مع بيانات المستخدم

### 4. SQL Injection
**التهديد:** حقن أوامر SQL ضارة (إذا تم ربط قاعدة بيانات)

**الحل الموصى به:**
- استخدام Prepared Statements
- عدم بناء استعلامات SQL من مدخلات المستخدم مباشرة
- التحقق من صحة جميع المدخلات

---

## 📋 قائمة التحقق الأمنية

قبل النشر الرسمي، تأكد من:

### ✅ المصادقة والصلاحيات
- [ ] تغيير جميع كلمات المرور الافتراضية
- [ ] إنشاء حسابات مستخدمين جديدة بكلمات مرور قوية
- [ ] حذف أو تعطيل الحسابات الافتراضية
- [ ] التحقق من صلاحيات كل دور

### ✅ مفاتيح API
- [ ] إزالة جميع مفاتيح API من الكود المصدري
- [ ] تكوين مفاتيح API عبر متغيرات البيئة
- [ ] التحقق من عدم وجود مفاتيح في Git History

### ✅ البيانات
- [ ] مراجعة البيانات الحساسة
- [ ] التأكد من حماية البيانات بنظام المصادقة
- [ ] إعداد نسخ احتياطية دورية

### ✅ الاتصال
- [ ] تفعيل HTTPS للموقع
- [ ] استخدام Secure Cookies
- [ ] تكوين CORS بشكل صحيح

### ✅ الكود
- [ ] مراجعة الكود للثغرات الأمنية
- [ ] إزالة console.log من الإنتاج
- [ ] تصغير وتشويش الكود (Minify & Obfuscate)

---

## 🔄 النسخ الاحتياطي

### استراتيجية النسخ الاحتياطي الموصى بها

1. **نسخ احتياطي يومي** لقاعدة البيانات
2. **نسخ احتياطي أسبوعي** للملفات والصور
3. **نسخ احتياطي شهري** كامل للنظام

### أدوات النسخ الاحتياطي

```bash
# نسخ احتياطي لقاعدة بيانات MySQL
mysqldump -u username -p database_name > backup_$(date +%Y%m%d).sql

# نسخ احتياطي للملفات
tar -czf backup_$(date +%Y%m%d).tar.gz /path/to/project
```

---

## 📞 الإبلاغ عن الثغرات الأمنية

إذا اكتشفت ثغرة أمنية في النظام:

1. **لا تنشرها علناً**
2. تواصل مع مدير النظام مباشرة
3. قدم تفاصيل الثغرة وخطوات إعادة إنتاجها
4. انتظر الرد والإصلاح قبل الكشف العلني

---

## 📚 موارد إضافية

- [OWASP Top 10](https://owasp.org/www-project-top-ten/) - أهم 10 مخاطر أمنية للتطبيقات
- [MDN Web Security](https://developer.mozilla.org/en-US/docs/Web/Security) - دليل أمان الويب
- [GitHub Security Best Practices](https://docs.github.com/en/code-security) - أفضل ممارسات الأمان على GitHub

---

## ⚖️ المسؤولية

- هذا النظام مخصص للاستخدام الداخلي لجامعة الإمام محمد بن سعود الإسلامية
- يجب احترام خصوصية البيانات الشخصية للسكان
- أي استخدام غير مصرح به للبيانات يعتبر انتهاكاً للخصوصية
- المستخدم مسؤول عن تأمين بيانات الدخول الخاصة به

---

**آخر تحديث:** 13 نوفمبر 2025  
**الإصدار:** 4.1  
**الحالة:** جاهز للنشر مع اتباع إرشادات الأمان
