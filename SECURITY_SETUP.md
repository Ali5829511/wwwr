# دليل إعداد الأمان - Security Setup Guide

## 🔒 نظرة عامة
هذا الدليل يشرح كيفية إعداد النظام بشكل آمن للنشر في بيئة الإنتاج.

---

## 🔐 خطوة 1: إعداد ملفات التكوين

### 1.1 إعداد Plate Recognizer API

قم بنسخ ملف التكوين النموذجي:
```bash
cp plate_recognizer_config.example.json plate_recognizer_config.json
```

ثم قم بتحرير `plate_recognizer_config.json` وإدخال بياناتك الفعلية:
```json
{
  "api_token": "أدخل رمز API الخاص بك هنا",
  "parkpow_token": "أدخل رمز ParkPow هنا",
  "ftp_username": "اسم المستخدم الخاص بك",
  "ftp_password": "كلمة المرور الخاصة بك"
}
```

**⚠️ تحذير مهم:** لا تقم أبداً بإضافة ملف `plate_recognizer_config.json` إلى Git!

---

## 🗄️ خطوة 2: إعداد قاعدة البيانات

### 2.1 الاختيار بين localStorage وقاعدة بيانات سحابية

#### الخيار أ: استخدام localStorage (للتطوير والاختبار فقط)
النظام حالياً يستخدم localStorage وهو جاهز للعمل مباشرة.

**المميزات:**
- ✅ لا يحتاج إعداد إضافي
- ✅ سريع ومباشر
- ✅ مناسب للتطوير

**العيوب:**
- ⚠️ البيانات محلية فقط على جهاز المستخدم
- ⚠️ محدود بـ 5-10 MB
- ⚠️ غير مناسب للإنتاج

#### الخيار ب: قاعدة بيانات سحابية (للإنتاج) ⭐ موصى به

##### استخدام Firebase Firestore:

1. **إنشاء مشروع Firebase:**
   - اذهب إلى [Firebase Console](https://console.firebase.google.com)
   - قم بإنشاء مشروع جديد
   - فعّل Firestore Database

2. **إضافة تكوين Firebase:**
   أنشئ ملف `firebase-config.js`:
   ```javascript
   const firebaseConfig = {
     apiKey: "YOUR_API_KEY",
     authDomain: "YOUR_AUTH_DOMAIN",
     projectId: "YOUR_PROJECT_ID",
     storageBucket: "YOUR_STORAGE_BUCKET",
     messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
     appId: "YOUR_APP_ID"
   };
   
   // تهيئة Firebase
   firebase.initializeApp(firebaseConfig);
   const db = firebase.firestore();
   ```

3. **تحديث database.js:**
   قم بتحديث الملف ليستخدم Firebase بدلاً من localStorage.

##### استخدام Supabase (بديل لـ Firebase):

1. **إنشاء مشروع Supabase:**
   - اذهب إلى [Supabase](https://supabase.com)
   - قم بإنشاء مشروع جديد
   - احصل على API URL و API Key

2. **إضافة تكوين Supabase:**
   ```javascript
   const supabaseUrl = 'YOUR_SUPABASE_URL';
   const supabaseKey = 'YOUR_SUPABASE_KEY';
   const supabase = createClient(supabaseUrl, supabaseKey);
   ```

3. **تنفيذ Schema:**
   استخدم `schema.sql` لإنشاء الجداول في Supabase.

---

## 🔑 خطوة 3: تغيير كلمات المرور الافتراضية

**⚠️ خطوة حرجة! يجب تنفيذها قبل النشر!**

### 3.1 تحديث كلمات المرور في database.js

افتح `database.js` وقم بتحديث كلمات المرور:

```javascript
// ابحث عن السطور التالية وقم بتغيير كلمات المرور:
{
    username: 'admin',
    password: 'كلمة_مرور_قوية_جديدة', // غير هذا!
    ...
},
{
    username: 'violations_officer',
    password: 'كلمة_مرور_قوية_جديدة', // غير هذا!
    ...
}
```

### 3.2 متطلبات كلمة المرور القوية:
- الحد الأدنى 12 حرف
- تحتوي على أحرف كبيرة وصغيرة
- تحتوي على أرقام
- تحتوي على رموز خاصة (@#$%^&*)

**مثال على كلمة مرور قوية:** `Admin@2025!Strong#Pass`

---

## 🛡️ خطوة 4: تفعيل HTTPS

### 4.1 للنشر على Netlify:
HTTPS يُفعّل تلقائياً! ✅

### 4.2 للنشر على خادم خاص:
استخدم Let's Encrypt للحصول على شهادة SSL مجانية:

```bash
# تثبيت Certbot
sudo apt-get install certbot python3-certbot-nginx

# الحصول على الشهادة
sudo certbot --nginx -d your-domain.com
```

---

## 📋 خطوة 5: قائمة التحقق الأمنية

قبل النشر، تأكد من:

### ✅ التكوينات:
- [ ] تم إنشاء ملف `.gitignore`
- [ ] تم إزالة `plate_recognizer_config.json` من Git
- [ ] تم إنشاء `plate_recognizer_config.example.json`
- [ ] تم تكوين جميع API tokens المطلوبة

### ✅ كلمات المرور:
- [ ] تم تغيير جميع كلمات المرور الافتراضية
- [ ] كلمات المرور قوية ومعقدة
- [ ] لا توجد كلمات مرور مكشوفة في الكود

### ✅ قاعدة البيانات:
- [ ] تم اختيار قاعدة البيانات المناسبة (localStorage أو سحابية)
- [ ] تم اختبار الاتصال بقاعدة البيانات
- [ ] تم إعداد النسخ الاحتياطية التلقائية

### ✅ HTTPS:
- [ ] تم تفعيل HTTPS
- [ ] تم اختبار الموقع عبر HTTPS
- [ ] لا توجد تحذيرات أمنية في المتصفح

### ✅ الأذونات:
- [ ] تم التحقق من صلاحيات المستخدمين
- [ ] تم تعطيل الحسابات غير المستخدمة
- [ ] تم اختبار نظام المصادقة

---

## 🚀 خطوة 6: النشر

### 6.1 النشر على Netlify (موصى به):

1. **ربط المستودع:**
   - اذهب إلى [Netlify](https://netlify.com)
   - قم بربط مستودع GitHub
   - اختر المستودع `Ali5829511/wwwr`

2. **تكوين النشر:**
   - Build command: `echo 'No build needed'`
   - Publish directory: `.`
   - البيئة: أضف متغيرات البيئة إن وُجدت

3. **النشر:**
   - انقر على "Deploy site"
   - انتظر اكتمال النشر
   - احصل على رابط الموقع

### 6.2 النشر على GitHub Pages:

راجع ملف [README.md](README.md) للتعليمات التفصيلية.

---

## 🔍 خطوة 7: الاختبار بعد النشر

### 7.1 اختبارات الأمان:
```bash
# اختبار HTTPS
curl -I https://your-site.netlify.app

# اختبار Headers الأمنية
curl -I https://your-site.netlify.app | grep -E "X-Frame|X-XSS|X-Content"
```

### 7.2 اختبارات الوظائف:
- [ ] تسجيل الدخول يعمل بشكل صحيح
- [ ] جميع الصفحات تُحمّل بدون أخطاء
- [ ] نظام الصلاحيات يعمل بشكل صحيح
- [ ] رفع الصور يعمل (إن كان مُفعّلاً)
- [ ] التنقل بين الصفحات سلس

---

## 📞 الدعم والمساعدة

إذا واجهت أي مشاكل:

1. راجع [SECURITY.md](SECURITY.md) للإرشادات الأمنية
2. راجع [README.md](README.md) للتوثيق الكامل
3. راجع [API_SETUP.md](API_SETUP.md) لإعداد APIs
4. تواصل مع مدير النظام

---

## 🎯 الخلاصة

**خطوات الأمان الأساسية:**

1. ✅ إزالة البيانات الحساسة من Git
2. ✅ تغيير كلمات المرور الافتراضية
3. ✅ استخدام قاعدة بيانات سحابية للإنتاج
4. ✅ تفعيل HTTPS
5. ✅ اختبار كامل للنظام
6. ✅ مراجعة دورية للأمان

**آخر تحديث:** 13 نوفمبر 2025  
**الإصدار:** 4.1  
**الحالة:** جاهز للنشر مع اتباع هذه الإرشادات
