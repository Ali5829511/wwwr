# دليل إعداد Plate Recognizer API

## 📸 نظرة عامة

هذا الدليل يشرح كيفية إعداد وتكوين **Plate Recognizer API** للتعرف الآلي على لوحات السيارات في نظام إدارة المرور.

---

## 🔑 الحصول على رمز API

### الخطوة 1: إنشاء حساب

1. اذهب إلى [Plate Recognizer](https://platerecognizer.com)
2. انقر على **"Sign Up"** أو **"Get Started"**
3. أكمل عملية التسجيل بالبريد الإلكتروني

### الخطوة 2: الحصول على الرمز

1. بعد تسجيل الدخول، اذهب إلى لوحة التحكم
2. انتقل إلى **"API"** أو **"Settings"**
3. انسخ **API Token** الخاص بك
4. احتفظ به في مكان آمن

### الخطوة 3: التحقق من الباقة

Plate Recognizer يقدم باقات مختلفة:

| الباقة | الطلبات الشهرية | السعر |
|--------|-----------------|-------|
| **Free** | 2,500 طلب | مجاني |
| **Starter** | 10,000 طلب | $39/شهر |
| **Professional** | 50,000 طلب | $149/شهر |
| **Enterprise** | غير محدود | حسب الطلب |

للاستخدام التجريبي، الباقة المجانية كافية.

---

## ⚙️ تكوين الرمز في النظام

لديك **3 طرق** لتكوين رمز API:

### الطريقة 1: عبر واجهة الإعدادات (موصى بها) ⭐

هذه الطريقة الأسهل والأكثر أماناً:

1. قم بتسجيل الدخول إلى النظام كـ **مدير**
2. من الصفحة الرئيسية، انقر على **"الإعدادات والخدمات"**
3. اختر **"إعدادات Webhook"**
4. في قسم **"إعدادات Plate Recognizer"**:
   - تأكد من تفعيل الخدمة (الزر الأخضر)
   - أدخل رمز API في حقل **"API Token"**
   - انقر على **"حفظ الإعدادات"**
5. انقر على **"اختبار الاتصال"** للتحقق من نجاح التكوين

**النتيجة:** سيتم حفظ الرمز في `localStorage` ويمكن استخدامه في جميع الصفحات.

---

### الطريقة 2: عبر Console المتصفح

للمطورين أو للتكوين السريع:

1. افتح النظام في المتصفح
2. اضغط `F12` لفتح Developer Tools
3. انتقل إلى تبويب **Console**
4. نفذ الأمر التالي:

```javascript
localStorage.setItem('PLATE_RECOGNIZER_API_TOKEN', 'YOUR_API_TOKEN_HERE');
```

5. استبدل `YOUR_API_TOKEN_HERE` برمز API الخاص بك
6. أعد تحميل الصفحة

**مثال:**
```javascript
localStorage.setItem('PLATE_RECOGNIZER_API_TOKEN', 'abc123xyz789token');
```

---

### الطريقة 3: عبر ملف تكوين منفصل (للنشر على خادم)

للنشر على خادم مع Backend:

1. أنشئ مجلد `config` في جذر المشروع (إذا لم يكن موجوداً)
2. أنشئ ملف `config/api-keys.js`:

```javascript
// config/api-keys.js
const API_KEYS = {
    PLATE_RECOGNIZER: 'YOUR_API_TOKEN_HERE'
};

// تصدير للاستخدام في الملفات الأخرى
if (typeof module !== 'undefined' && module.exports) {
    module.exports = API_KEYS;
}
```

3. في `advanced_vehicle_analyzer.html`, قم بتحميل الملف:

```html
<script src="../config/api-keys.js"></script>
<script>
    const API_TOKEN = API_KEYS.PLATE_RECOGNIZER;
</script>
```

4. تأكد من إضافة `config/api-keys.js` إلى `.gitignore`:

```
# في ملف .gitignore
config/api-keys.js
```

---

## 🧪 اختبار التكوين

### عبر واجهة النظام

1. اذهب إلى **"أنظمة التعرف الذكي"** → **"تحليل صور السيارات المتقدم"**
2. ارفع صورة لوحة سيارة
3. انقر على **"تحليل الصور"**
4. إذا نجح التحليل، سترى:
   - رقم اللوحة
   - المنطقة
   - الحروف
   - نسبة الثقة

### عبر API مباشرة

يمكنك اختبار الرمز مباشرة عبر cURL:

```bash
curl -X POST \
  https://api.platerecognizer.com/v1/plate-reader/ \
  -H 'Authorization: Token YOUR_API_TOKEN_HERE' \
  -F 'upload=@/path/to/car-image.jpg' \
  -F 'regions=sa'
```

**مثال على الاستجابة الناجحة:**
```json
{
  "processing_time": 123.45,
  "results": [
    {
      "plate": "أ ب ج 1234",
      "region": {
        "code": "sa"
      },
      "vehicle": {
        "type": "Sedan"
      },
      "score": 0.95
    }
  ]
}
```

---

## 🚨 استكشاف الأخطاء

### الخطأ: "API Token is not set"

**السبب:** لم يتم تكوين رمز API

**الحل:**
1. تأكد من تنفيذ إحدى طرق التكوين أعلاه
2. تحقق من `localStorage` في Console:
```javascript
console.log(localStorage.getItem('PLATE_RECOGNIZER_API_TOKEN'));
```
3. إذا كان `null`، قم بتعيينه مرة أخرى

---

### الخطأ: "401 Unauthorized"

**السبب:** رمز API غير صحيح أو منتهي الصلاحية

**الحل:**
1. تحقق من صحة الرمز في لوحة تحكم Plate Recognizer
2. تأكد من نسخ الرمز بالكامل بدون مسافات
3. جرب إنشاء رمز جديد

---

### الخطأ: "429 Too Many Requests"

**السبب:** تجاوزت حد الطلبات في باقتك

**الحل:**
1. تحقق من استهلاكك الشهري في لوحة التحكم
2. انتظر حتى إعادة تعيين الحد الشهري
3. أو قم بالترقية إلى باقة أعلى

---

### الخطأ: "No plates detected"

**السبب:** الصورة غير واضحة أو لا تحتوي على لوحة

**الحل:**
1. تأكد من جودة الصورة (دقة عالية)
2. تأكد من وضوح اللوحة في الصورة
3. تأكد من الإضاءة الجيدة
4. جرب صورة أخرى

---

## 📊 أفضل الممارسات

### 1. إدارة الحصة (Quota)

- راقب استهلاكك الشهري
- استخدم التحليل فقط عند الحاجة
- قم بتخزين النتائج لتجنب التحليل المتكرر

### 2. جودة الصور

للحصول على أفضل نتائج:
- **الدقة:** 1280x720 بكسل على الأقل
- **الإضاءة:** إضاءة جيدة ومتساوية
- **الزاوية:** لوحة واضحة ومواجهة للكاميرا
- **التركيز:** صورة واضحة وغير ضبابية

### 3. الأمان

- **لا تشارك** رمز API علناً
- **لا تضمن** الرمز في الكود المصدري
- **استخدم** متغيرات البيئة أو localStorage
- **قم بتجديد** الرمز بشكل دوري

### 4. التخزين المؤقت

لتقليل الطلبات وتحسين الأداء:
- قم بتخزين النتائج في قاعدة البيانات
- استخدم التخزين المؤقت للصور المحللة
- تجنب تحليل نفس الصورة مرتين

---

## 🔗 الموارد الإضافية

- **التوثيق الرسمي:** https://docs.platerecognizer.com
- **API Reference:** https://docs.platerecognizer.com/api-reference
- **الدعم الفني:** support@platerecognizer.com
- **لوحة التحكم:** https://app.platerecognizer.com

---

## 💡 نصائح متقدمة

### استخدام Regions للتحسين

حدد المنطقة لتحسين الدقة:

```javascript
formData.append('regions', 'sa'); // السعودية
formData.append('regions', 'ae'); // الإمارات
formData.append('regions', 'kw'); // الكويت
```

### معالجة متعددة

لتحليل عدة صور دفعة واحدة:

```javascript
const promises = images.map(image => analyzeImage(image));
const results = await Promise.all(promises);
```

### التعامل مع الأخطاء

```javascript
try {
    const result = await analyzeImage(file);
    if (result.results.length === 0) {
        console.warn('لم يتم اكتشاف لوحات في الصورة');
    }
} catch (error) {
    if (error.status === 429) {
        alert('تجاوزت حد الطلبات. يرجى المحاولة لاحقاً.');
    } else {
        console.error('خطأ في التحليل:', error);
    }
}
```

---

**آخر تحديث:** 13 نوفمبر 2025  
**الإصدار:** 4.1  
**الحالة:** جاهز للاستخدام
